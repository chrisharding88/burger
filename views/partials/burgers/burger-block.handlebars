<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newBurgerEat="{{devoured}}">
		{{#if devoured}}Devoured! Mhm...Delicious{{else}}Ewww...me no likey{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
